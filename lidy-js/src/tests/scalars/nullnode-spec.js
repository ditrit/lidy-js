import { parse } from '../../parser/node_parse.js'
/*
describe("Lidy scalars ->", function() {

    describe("null scalar : ", function() {
        
        it("null symbol",
            function() { expect( parse({src_data: "~", dsl_data: "main: ~"}).result().value).toEqual(null)})

        it("null value",
            function() { expect( parse({src_data: "null", dsl_data: "main: ~"}).result().value).toEqual(null)})

        it("int is not null",
            function() { expect( parse({src_data: "0", dsl_data: "main: ~"}).fails()).toEqual(true)})

        it("string is not null",
            function() { expect( parse({src_data: `"~ "`, dsl_data: "main: ~"}).fails()).toEqual(true)})

        it("empty list is not null",
            function() { expect( parse({src_data: "[]", dsl_data: "main: ~"}).fails()).toEqual(true)})

        it("empty map is not null",
            function() { expect( parse({src_data: "{}", dsl_data: "main: ~"}).fails()).toEqual(true)})

        it("empty string is not null",
            function() { expect( parse({src_data: '""', dsl_data: "main: ~"}).fails()).toEqual(true)})

    })
})
*/